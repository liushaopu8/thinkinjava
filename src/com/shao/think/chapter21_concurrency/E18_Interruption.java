/**********************************************
 * Create a non-task class with a method that calls
 * sleep() for a long interval. Create a task that
 * calls the method in the non-task class. In main()
 * start the task, then call interrupt() to terminate
 * it. Make sure taht the task shuts down safely
 **********************************************/
package com.shao.think.chapter21_concurrency;

public class E18_Interruption {
}
