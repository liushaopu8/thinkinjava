/**********************************************
 * Create a class with three methods containing
 * critical sections that all synchronize on the
 * same object. Create multiple tasks to demonstrate
 * only one of these methods can run at a time.
 * Now modify the methods so that each one
 * synchronizes on a different object and show
 * that all three methods can be running at once.
 **********************************************/
package com.shao.think.chapter21_concurrency;

public class E15_SyncObject {
}
